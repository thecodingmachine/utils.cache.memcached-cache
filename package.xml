<?xml version="1.0" encoding="UTF-8"?>
<package>
	<displayName>Memcached Cache</displayName>
	<shortDescription>This package contains a cache mechanism that relies on the Memcached database.
	For this cache to work, the memcached Pecl package must be enabled.</shortDescription>
	<docUrl></docUrl>
	<logo>logo.png</logo>
	<dependencies>
		<dependency>
			<group>utils/cache</group>
			<name>cache-interface</name>
			<version>1.0</version>
		</dependency>
	</dependencies>
	<requires>
		<require>MemcachedCache.php</require>
	</requires>
</package>